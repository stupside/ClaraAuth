#include <iostream>

namespace hardware {
    std::string computer_sid();
    std::string processor_name();
    std::string username();
    std::string base_board();
    std::string physical_memory();
    std::string computer_name();
}